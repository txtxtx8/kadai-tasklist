<h1>id: <%= @task.id %> のタスクを修正する</h1>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'タスクの内容' %>
  <%= f.text_field :content %>

  <%= f.submit '完了' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>